{
  "name": "bounce-firebase",
  "version": "0.0.1",
  "description": "ricko.is bounce redirector, firebase version",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.16.4",
    "firebase-functions": "^2.1.0",
    "lodash": "^4.17.5"
  },
  "devDependencies": {
    "firebase-tools": "*"
  }
}
